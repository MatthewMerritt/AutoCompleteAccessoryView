language: objective-c
osx_image: xcode11.3
# cache: cocoapods
# podfile: Example/Podfile
# before_install:
# - gem install cocoapods # Since Travis is not always on latest version
# - pod install --project-directory=Example
script:
- set -o pipefail && xcodebuild -enableCodeCoverage YES -workspace Example/AutoCompleteAccessoryView.xcworkspace -scheme AutoCompleteAccessoryView-Example -sdk iphonesimulator13.2 ONLY_ACTIVE_ARCH=NO | xcpretty
- pod lib lint
