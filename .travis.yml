language: objective-c
#osx_image: xcode11
#name: "iPhone X (iOS 13.0)"

# cache: cocoapods
# podfile: Example/Podfile
# before_install:
# - gem install cocoapods # Since Travis is not always on latest version
# - pod install --project-directory=Example
script:
    # - set -o pipefail && xcodebuild test -enableCodeCoverage YES -workspace Example/AutoCompleteAccessoryView.xcworkspace -scheme AutoCompleteAccessoryView-Example -sdk iphonesimulator12.2 ONLY_ACTIVE_ARCH=NO | xcpretty
    #- pod lib lint

jobs:
    include:

        - stage: "Tests"

        - script: xcodebuild clean test -scheme AutoCompleteAccessoryView -workspace Example/AutoCompleteAccessoryView.xcworkspace -destination 'platform=iOS Simulator,OS=13.0,name=iPhone 11'
          osx_image: xcode11
          name: "iPhone X (iOS 13.0)"

        - stage: CocoaPods
          osx_image: xcode11
          before_install: 
            - gem install cocoapods
          script:
            - pod spec lint --allow-warnings
            - pod lib lint --allow-warnings
